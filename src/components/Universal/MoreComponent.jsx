import More from '../UI/More'
import Divider from '../UI/Divider'

const MoreComponent = ({title}) => {
  return (
    <>
        <More title={title} />
        <Divider />
    </>
  )
}

export default MoreComponent
